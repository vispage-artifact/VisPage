"""Experiment 2: locality and page construction analysis.

This experiment is intentionally not wired until its real metrics are migrated.
It must not call runtime code or produce substitute results.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError(
        "Exp2 requires real offline locality metrics before it can be run: "
        "coverage, random/embedding/oracle comparison, read amplification, and cache pressure."
    )


if __name__ == "__main__":
    main()

